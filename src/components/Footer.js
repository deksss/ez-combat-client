import React from "react";

const style = {
  flex: 1,
  display: "flex",
  justifyContent: "flex-end",
  flexDirection: "column",
  padding: 20,
  paddingBottom: 5,
  color: "#777"
};

const Footer = props => (
  <div style={style}>
    <div>Deksss - 2017</div>
  </div>
);

export default Footer;
